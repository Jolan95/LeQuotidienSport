{% extends 'base.html.twig' %}

{% block title %}Réinitialisation du mot de passe{% endblock %}

{% block body %}
<div class="page-100 full-center py-4" style="background-image : url('{{asset("image/background-race.jpg")}}')">
<div class="wrapper-form wrapper-form-xl px-4 py-3">
    <h1>Réinitialisez votre mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Réinitialiser le mot de passe</button>
    {{ form_end(resetForm) }}
    </div>
    </div>
{% endblock %}
